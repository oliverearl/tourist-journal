"use strict";
/**
 * Fill Details
 * @param id
 *
 * Passes on data to the Get Entry function. There used to be more here. Now it's just a phantom.
 */
const fillDetails = function(id) {
  Conference.dataContext.getEntry(id);
};
